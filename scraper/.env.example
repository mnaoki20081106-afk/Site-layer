# Either set SITE1_URL directly...
SITE1_URL=

# ...or point at this project's own /api/config endpoint so the scraper
# always follows whatever site1Url is currently set in the admin panel.
CONFIG_API_URL=https://<your-worker>.workers.dev/api/config
